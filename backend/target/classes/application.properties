# Import Environment Variables from config.properties file
spring.config.import=optional:file:config.properties

# Spring Boot Application Configuration
spring.application.name=Employee-Management

# MySQL Database Configuration (with env variables)
# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/employee_management?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Babu@mb16

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# MongoDB Configuration (with env variable)
spring.data.mongodb.uri=mongodb+srv://maheshbabumb08_db_user:XjGLF0fbSLuEjnV4@cluster1.jk1gyxj.mongodb.net/employee_management?retryWrites=true&w=majority&appName=Cluster1

# Server Configuration
server.port=8080
